#include<stdio.h>
int main()
{
	//multiply two floating point numbers.
	float a,b;
	printf("Enter two floating point numbers:");
	scanf("%g %g",&a,&b);
	printf("Product=%g\n",a*b);
	return 0;

}